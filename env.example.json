{
  "private_key_path": "path/to/your/private/key", // e.g., "~/.ssh/id_rsa"
  "projects": [
    {
      "name": "Your Project Name", // e.g., "Dummy app"
      "local_dir": "path/to/your/local/directory", // e.g., "~/Downloads"
      "config": {
        "environments": [
          {
            "name": "Environment Name", // e.g., "develop"
            "server_ip": "Server IP Address", // e.g., "123.45.67.89"
            "server_user": "Server Username", // e.g., "root"
            "db_connection_string": "Database Connection String", // e.g., "postgresql://user:password@host:port/database?sslmode=require"
            "dump_filename": "Database Dump Filename" // e.g., "database_dump.sql"
          },
        ]
      }
    }
  ]
}
